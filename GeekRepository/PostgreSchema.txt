# PostgreSQL schema (GeekRepository shared instance)

## Content tables (EF Core / AppDbContext)

- departments, case_studies, case_study_metrics, case_study_actors, case_study_event_flow_steps, use_cases

## Product SEO (EF — GeekSeo.Persistence)

- Schema `geek_seo` — owned by Geek-SEO repo; applied at GeekRepository startup via `SeoDbContext`

## Legacy platform auth (Dapper) — DROPPED

Migration `0006_drop_legacy_platform_auth.sql` removes public tables: users, user_secrets, user_claims,
devices_oauth, two_factor_*, sync_*, audit_log, security_incidents, device_reregistration_requests,
pending_verifications, device_registration_codes, roles, permissions, user_roles, role_permissions,
websocket_sessions.

Identity for Geek products: **GeekOAuth** (`asp_net_users` in geek_oauth database).

## Already removed (0004)

OpenIddict issuer tables, jti_blacklist, oidc_storage, geek_auth adapter PascalCase tables.

## Do not drop via GeekRepository

- auth.oauth_clients, auth.sessions (Supabase Auth — dashboard only)
- schema_migrations (SQL runner bookkeeping)
